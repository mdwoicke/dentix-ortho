# =============================================================================
# Test Agent Configuration
# =============================================================================

# Flowise Chatbot Endpoint (required for E2E tests)
# FLOWISE_ENDPOINT=https://your-flowise-instance/api/v1/prediction/your-flow-id

# =============================================================================
# LLM / AI Configuration
# =============================================================================

# Replit Mode - Set to 'true' when deploying on Replit
# Automatically disables Claude CLI and uses API-only mode
REPLIT_MODE=false

# Claude CLI mode (ignored when REPLIT_MODE=true)
# Set to 'true' to use Claude CLI for AI evaluation (local development)
# OAuth tokens are loaded dynamically from ~/.claude/.credentials.json
USE_CLAUDE_CLI=true

# Anthropic API Key (required when REPLIT_MODE=true or USE_CLAUDE_CLI=false)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OAuth Token fallback (optional, auto-loaded from ~/.claude/.credentials.json)
# CLAUDE_CODE_OAUTH_TOKEN=

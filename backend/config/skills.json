{
  "skills": [
    {
      "id": "echo-test",
      "name": "Echo Test",
      "description": "Simple echo command for testing SSH connection",
      "command": "echo 'Hello from SSH! Connection successful.'",
      "category": "utility",
      "inputs": []
    },
    {
      "id": "system-info",
      "name": "System Info",
      "description": "Display system information (OS, uptime, memory)",
      "command": "uname -a && echo '---' && uptime && echo '---' && free -h 2>/dev/null || echo 'Memory info not available'",
      "category": "utility",
      "inputs": []
    },
    {
      "id": "git-status",
      "name": "Git Status",
      "description": "Show git status with file changes summary",
      "command": "cd {{workDir}} && git status && echo '--- Recent Commits ---' && git log --oneline -5",
      "category": "git",
      "inputs": []
    },
    {
      "id": "git-diff",
      "name": "Git Diff",
      "description": "Show uncommitted changes",
      "command": "cd {{workDir}} && git diff --stat && echo '--- Details ---' && git diff",
      "category": "git",
      "inputs": []
    },
    {
      "id": "git-commit-simple",
      "name": "Git Commit (Simple)",
      "description": "Stage all changes and commit with a message (no AI)",
      "command": "cd {{workDir}} && git add -A && git commit -m \"{{message}}\"",
      "category": "git",
      "inputs": [
        {
          "name": "message",
          "label": "Commit Message",
          "type": "text",
          "required": true,
          "placeholder": "feat: add new feature",
          "description": "The commit message (use conventional commits format)"
        }
      ]
    },
    {
      "id": "git-commit-auto",
      "name": "Git Commit (Auto Message)",
      "description": "Stage all changes and commit with auto-generated message based on diff",
      "command": "cd {{workDir}} && git add -A && git diff --cached --stat | head -20 && echo '---' && git commit -m \"$(git diff --cached --stat | head -1 | sed 's/^ *//' | cut -d'|' -f1 | xargs echo 'Update:')\"",
      "category": "git",
      "inputs": []
    },
    {
      "id": "git-push",
      "name": "Git Push",
      "description": "Push commits to remote repository",
      "command": "cd {{workDir}} && git push {{remote}} {{branch}}",
      "category": "git",
      "inputs": [
        {
          "name": "remote",
          "label": "Remote",
          "type": "text",
          "required": false,
          "default": "origin",
          "description": "Remote name (default: origin)"
        },
        {
          "name": "branch",
          "label": "Branch",
          "type": "text",
          "required": false,
          "default": "",
          "description": "Branch name (leave empty for current branch)"
        }
      ]
    },
    {
      "id": "git-log",
      "name": "Git Log",
      "description": "Show recent commit history",
      "command": "cd {{workDir}} && git log --oneline --graph -{{count}}",
      "category": "git",
      "inputs": [
        {
          "name": "count",
          "label": "Number of Commits",
          "type": "number",
          "required": false,
          "default": 20,
          "min": 1,
          "max": 100,
          "description": "How many commits to show"
        }
      ]
    },
    {
      "id": "run-tests",
      "name": "Run Tests",
      "description": "Execute test-agent test scenarios",
      "command": "cd {{workDir}} && npm run run -- {{scenariosArg}} {{concurrencyArg}}",
      "category": "test-agent",
      "inputs": [
        {
          "name": "scenarios",
          "label": "Scenarios",
          "type": "text",
          "required": false,
          "placeholder": "GOAL-HAPPY-001,GOAL-HAPPY-002",
          "description": "Comma-separated list of scenario IDs to run (leave empty for all)"
        },
        {
          "name": "concurrency",
          "label": "Concurrency",
          "type": "number",
          "required": false,
          "default": 5,
          "min": 1,
          "max": 20,
          "description": "Number of parallel test workers"
        }
      ]
    },
    {
      "id": "diagnose",
      "name": "Diagnose Failures",
      "description": "Analyze test failures and generate fix recommendations",
      "command": "cd {{workDir}} && npm run diagnose",
      "category": "test-agent",
      "inputs": []
    },
    {
      "id": "ab-run",
      "name": "Run A/B Experiment",
      "description": "Execute an A/B testing experiment",
      "command": "cd {{workDir}} && npm run ab-run -- {{experimentId}} {{iterationsArg}}",
      "category": "test-agent",
      "inputs": [
        {
          "name": "experimentId",
          "label": "Experiment ID",
          "type": "text",
          "required": true,
          "placeholder": "exp-123",
          "description": "The ID of the experiment to run"
        },
        {
          "name": "iterations",
          "label": "Iterations",
          "type": "number",
          "required": false,
          "default": 10,
          "min": 1,
          "max": 100,
          "description": "Number of iterations per variant"
        }
      ]
    },
    {
      "id": "ab-status",
      "name": "A/B Experiment Status",
      "description": "View status of A/B experiments",
      "command": "cd {{workDir}} && npm run ab-status {{experimentIdArg}}",
      "category": "test-agent",
      "inputs": [
        {
          "name": "experimentId",
          "label": "Experiment ID (optional)",
          "type": "text",
          "required": false,
          "placeholder": "exp-123",
          "description": "Leave empty to see all experiments"
        }
      ]
    },
    {
      "id": "scenarios-list",
      "name": "List Scenarios",
      "description": "List all available test scenarios",
      "command": "cd {{workDir}} && npm run scenarios",
      "category": "test-agent",
      "inputs": []
    },
    {
      "id": "results",
      "name": "View Results",
      "description": "View test results from previous runs",
      "command": "cd {{workDir}} && npm run results {{limitArg}}",
      "category": "test-agent",
      "inputs": [
        {
          "name": "limit",
          "label": "Limit",
          "type": "number",
          "required": false,
          "default": 10,
          "min": 1,
          "max": 100,
          "description": "Number of results to show"
        }
      ]
    },
    {
      "id": "custom-command",
      "name": "Custom Command",
      "description": "Run any custom shell command",
      "command": "{{command}}",
      "category": "custom",
      "inputs": [
        {
          "name": "command",
          "label": "Command",
          "type": "textarea",
          "required": true,
          "placeholder": "ls -la",
          "description": "The shell command to execute"
        }
      ]
    },
    {
      "id": "claude-cli-llm",
      "name": "Claude CLI (LLM Service)",
      "description": "Run Claude CLI via LLM service - handles stdin properly on Windows",
      "command": "cd {{workDir}} && npm run llm-prompt -- --prompt \"{{prompt}}\" --model {{model}}",
      "category": "ai",
      "inputs": [
        {
          "name": "prompt",
          "label": "Prompt",
          "type": "textarea",
          "required": true,
          "placeholder": "Explain what this code does...",
          "description": "The prompt to send to Claude"
        },
        {
          "name": "model",
          "label": "Model",
          "type": "select",
          "required": false,
          "default": "sonnet",
          "options": [
            { "value": "sonnet", "label": "Claude Sonnet (Fast)" },
            { "value": "opus", "label": "Claude Opus (Best)" },
            { "value": "haiku", "label": "Claude Haiku (Fastest)" }
          ],
          "description": "Which Claude model to use"
        }
      ]
    },
    {
      "id": "claude-cli",
      "name": "Claude CLI (Raw)",
      "description": "Run Claude CLI directly (may have issues on Windows)",
      "command": "claude --print --model {{model}} -p \"{{prompt}}\"",
      "category": "ai",
      "inputs": [
        {
          "name": "prompt",
          "label": "Prompt",
          "type": "textarea",
          "required": true,
          "placeholder": "Explain what this code does...",
          "description": "The prompt to send to Claude"
        },
        {
          "name": "model",
          "label": "Model",
          "type": "select",
          "required": false,
          "default": "sonnet",
          "options": [
            { "value": "sonnet", "label": "Claude Sonnet (Fast)" },
            { "value": "opus", "label": "Claude Opus (Best)" },
            { "value": "haiku", "label": "Claude Haiku (Fastest)" }
          ],
          "description": "Which Claude model to use"
        }
      ]
    },
    {
      "id": "claude-analyze-file",
      "name": "Claude Analyze File",
      "description": "Use Claude to analyze a file in the project",
      "command": "cd {{workDir}} && claude --print --model {{model}} -p \"Analyze this file and explain what it does: $(cat {{filePath}})\"",
      "category": "ai",
      "inputs": [
        {
          "name": "filePath",
          "label": "File Path",
          "type": "text",
          "required": true,
          "placeholder": "src/index.ts",
          "description": "Relative path to the file to analyze"
        },
        {
          "name": "model",
          "label": "Model",
          "type": "select",
          "required": false,
          "default": "sonnet",
          "options": [
            { "value": "sonnet", "label": "Claude Sonnet (Fast)" },
            { "value": "haiku", "label": "Claude Haiku (Fastest)" }
          ],
          "description": "Which Claude model to use"
        }
      ]
    },
    {
      "id": "claude-skill-runner",
      "name": "Run Claude Skill File (API)",
      "description": "Execute a Claude skill from a .MD file using the LLM Provider (uses API credits)",
      "skillType": "claude-skill-file",
      "category": "ai",
      "inputs": [
        {
          "name": "skillFilePath",
          "label": "Skill File",
          "type": "text",
          "required": true,
          "placeholder": ".claude/skills/e2e-test.md",
          "description": "Path to the .MD skill file (relative to project root)"
        },
        {
          "name": "userPrompt",
          "label": "User Prompt",
          "type": "textarea",
          "required": true,
          "placeholder": "What would you like me to do?",
          "description": "Your request/instruction for the skill"
        },
        {
          "name": "model",
          "label": "Model",
          "type": "select",
          "required": false,
          "default": "sonnet",
          "options": [
            { "value": "sonnet", "label": "Claude Sonnet (Fast)" },
            { "value": "opus", "label": "Claude Opus (Best)" },
            { "value": "haiku", "label": "Claude Haiku (Fastest)" }
          ],
          "description": "Which Claude model to use"
        }
      ]
    },
    {
      "id": "claude-skill-runner-pty",
      "name": "Run Claude Skill File (PTY)",
      "description": "Execute a Claude skill from a .MD file using PTY (uses subscription auth)",
      "skillType": "pty-skill-file",
      "category": "ai",
      "inputs": [
        {
          "name": "skillFilePath",
          "label": "Skill File",
          "type": "text",
          "required": true,
          "placeholder": ".claude/skills/e2e-test.md",
          "description": "Path to the .MD skill file (relative to project root)"
        },
        {
          "name": "userPrompt",
          "label": "User Prompt",
          "type": "textarea",
          "required": true,
          "placeholder": "What would you like me to do?",
          "description": "Your request/instruction for the skill"
        }
      ]
    },
    {
      "id": "claude-plugin",
      "name": "Run Claude Plugin (PTY)",
      "description": "Execute a Claude Code plugin in interactive mode using PTY (uses subscription auth)",
      "command": "claude {{plugin}}",
      "skillType": "pty",
      "category": "ai",
      "inputs": [
        {
          "name": "plugin",
          "label": "Plugin Command",
          "type": "text",
          "required": true,
          "placeholder": "/commit",
          "description": "The plugin command to run (e.g., /commit, /reflect, /ralph-loop)"
        }
      ]
    },
    {
      "id": "claude-plugin-print",
      "name": "Run Claude Plugin (API)",
      "description": "Execute a Claude Code plugin in non-interactive mode (uses API credits)",
      "command": "cd {{workDir}} && claude --print --permission-mode dontAsk {{plugin}}",
      "category": "ai",
      "inputs": [
        {
          "name": "plugin",
          "label": "Plugin Command",
          "type": "text",
          "required": true,
          "placeholder": "/commit",
          "description": "The plugin command to run (e.g., /commit, /reflect, /ralph-loop)"
        }
      ]
    },
    {
      "id": "nodered-deploy",
      "name": "Deploy Node-RED Flow",
      "description": "Push docs/v1/nodered_Cloud9_flows.json to production Node-RED",
      "command": "cd {{workDir}} && node scripts/deploy-nodered.js {{backupArg}} {{dryRunArg}}",
      "category": "deployment",
      "inputs": [
        {
          "name": "backup",
          "label": "Create Backup First",
          "type": "checkbox",
          "default": true,
          "description": "Save current flows before deploying"
        },
        {
          "name": "dryRun",
          "label": "Dry Run (validate only)",
          "type": "checkbox",
          "default": false,
          "description": "Validate connection and file without deploying"
        }
      ]
    },
    {
      "id": "nodered-list-flows",
      "name": "List Node-RED Flows",
      "description": "List all available flow tabs in Node-RED",
      "command": "cd {{workDir}} && node scripts/copy-nodered-flow.js --list",
      "category": "deployment",
      "inputs": []
    },
    {
      "id": "nodered-copy-flow",
      "name": "Copy Node-RED Flow",
      "description": "Copy an existing Node-RED flow to a new flow with a new name",
      "command": "cd {{workDir}} && node scripts/copy-nodered-flow.js --source \"{{source}}\" --name \"{{newName}}\" {{disabledArg}} {{backupArg}} {{dryRunArg}}",
      "category": "deployment",
      "inputs": [
        {
          "name": "source",
          "label": "Source Flow (ID or Label)",
          "type": "text",
          "required": true,
          "placeholder": "Chord-Cloud9-Ortho-Prd",
          "description": "The ID or label of the flow to copy"
        },
        {
          "name": "newName",
          "label": "New Flow Name",
          "type": "text",
          "required": true,
          "placeholder": "Chord-Cloud9-Ortho-Dev",
          "description": "Label for the new copied flow"
        },
        {
          "name": "disabled",
          "label": "Create as Disabled",
          "type": "checkbox",
          "default": false,
          "description": "Create the new flow in disabled state"
        },
        {
          "name": "backup",
          "label": "Create Backup First",
          "type": "checkbox",
          "default": true,
          "description": "Save current flows before copying"
        },
        {
          "name": "dryRun",
          "label": "Dry Run (validate only)",
          "type": "checkbox",
          "default": false,
          "description": "Validate without deploying"
        }
      ]
    },
    {
      "id": "total-call-trace",
      "name": "Total Call Trace",
      "description": "Comprehensive Langfuse session analysis - traces all tool calls, investigates slot metadata, detects failure patterns (FP-001 through FP-006), and suggests solutions",
      "command": "cd {{workDir}} && node scripts/total-call-trace.js {{sessionId}}",
      "category": "agents",
      "inputs": [
        {
          "name": "sessionId",
          "label": "Langfuse Session ID",
          "type": "text",
          "required": true,
          "placeholder": "30dc66e1-eabd-46ca-b546-9a4237e64f19",
          "description": "The Langfuse session ID to analyze (from Ortho-Test-JL-UAT project)"
        }
      ]
    },
    {
      "id": "agent-total-session-trace",
      "name": "Total Session Trace",
      "description": "Full tool call dump with execution times, cache decisions, and timing summary per tool",
      "command": "cd {{workDir}} && node scripts/total-session-trace.js {{sessionId}}",
      "category": "agents",
      "inputs": [
        {
          "name": "sessionId",
          "label": "Session ID",
          "type": "text",
          "required": true,
          "placeholder": "e.g. 7dd08971-bdcf-4b2f-8c39-a1896846adda"
        }
      ]
    },
    {
      "id": "agent-debug-session-quick",
      "name": "Quick Session Debug",
      "description": "Fast session diagnostics - checks tool calls, errors, and basic failure patterns",
      "command": "cd {{workDir}} && node scripts/debug-session-quick.js {{sessionId}}",
      "category": "agents",
      "inputs": [
        {
          "name": "sessionId",
          "label": "Session ID",
          "type": "text",
          "required": true,
          "placeholder": "e.g. 30dc66e1-eabd-46ca-b546-9a4237e64f19"
        }
      ]
    },
    {
      "id": "agent-analyze-session",
      "name": "Analyze Session",
      "description": "Session analysis with tool call extraction and comparison against known patterns",
      "command": "cd {{workDir}} && node scripts/analyze-session.js {{sessionId}}",
      "category": "agents",
      "inputs": [
        {
          "name": "sessionId",
          "label": "Session ID",
          "type": "text",
          "required": true,
          "placeholder": "e.g. 30dc66e1-eabd-46ca-b546-9a4237e64f19"
        }
      ]
    },
    {
      "id": "agent-trace-session",
      "name": "Trace Session",
      "description": "Trace all Langfuse observations for a session with chronological tool call log",
      "command": "cd {{workDir}} && node scripts/trace-session.js {{sessionId}}",
      "category": "agents",
      "inputs": [
        {
          "name": "sessionId",
          "label": "Session ID",
          "type": "text",
          "required": true,
          "placeholder": "e.g. 30dc66e1-eabd-46ca-b546-9a4237e64f19"
        }
      ]
    },
    {
      "id": "agent-analyze-failure-pattern",
      "name": "Analyze Failure Patterns",
      "description": "Deep-dive failure pattern analysis across recent test runs",
      "command": "cd {{workDir}} && node scripts/analyze-failure-pattern.js {{sessionId}}",
      "category": "agents",
      "inputs": [
        {
          "name": "sessionId",
          "label": "Session ID (optional)",
          "type": "text",
          "required": false,
          "placeholder": "Leave empty for recent failures"
        }
      ]
    },
    {
      "id": "agent-analyze-sibling-failures",
      "name": "Analyze Sibling Failures",
      "description": "Analyze sibling booking failures - multi-child scheduling race conditions and token issues",
      "command": "cd {{workDir}} && node scripts/analyze-sibling-failures.js",
      "category": "agents",
      "inputs": []
    },
    {
      "id": "agent-full-session-analysis",
      "name": "Full Session Analysis",
      "description": "Complete session analysis including all observations, LLM interactions, and tool calls",
      "command": "cd {{workDir}} && node scripts/full-session-analysis.js {{sessionId}}",
      "category": "agents",
      "inputs": [
        {
          "name": "sessionId",
          "label": "Session ID",
          "type": "text",
          "required": true,
          "placeholder": "e.g. 30dc66e1-eabd-46ca-b546-9a4237e64f19"
        }
      ]
    },
    {
      "id": "agent-analyze-recent-failures",
      "name": "Analyze Recent Failures",
      "description": "Scan recent test runs for failures and categorize by error type",
      "command": "cd {{workDir}} && node scripts/analyze-recent-failures.js",
      "category": "agents",
      "inputs": []
    },
    {
      "id": "agent-analyze-trace",
      "name": "Analyze Trace",
      "description": "Analyze a specific Langfuse trace by trace ID",
      "command": "cd {{workDir}} && node scripts/analyze-trace.js {{traceId}}",
      "category": "agents",
      "inputs": [
        {
          "name": "traceId",
          "label": "Trace ID",
          "type": "text",
          "required": true,
          "placeholder": "e.g. dd1a7852-e45f-4e89-b09e-2e633318f5d0"
        }
      ]
    },
    {
      "id": "chord-call-lookup",
      "name": "Chord Call Lookup",
      "description": "Search for a Chord DSO call by any ID (trace ID, session ID, phone number, GUID). Returns formatted session ID, booking details, and call summary.",
      "command": "cd {{workDir}} && node \"C:/Users/mwoic/.claude/skills/chord-call-lookup/lookup.js\" {{searchId}} {{daysArg}} {{allConfigsArg}}",
      "category": "agents",
      "inputs": [
        {
          "name": "searchId",
          "label": "Search ID",
          "type": "text",
          "required": true,
          "placeholder": "e.g. +12132791621 or trace UUID or session ID",
          "description": "Any identifier: phone number, trace ID, session ID, appointment ID, GUID"
        },
        {
          "name": "days",
          "label": "Days Back",
          "type": "number",
          "required": false,
          "default": 30,
          "min": 1,
          "max": 90,
          "description": "How many days back to search"
        },
        {
          "name": "allConfigs",
          "label": "Search All Configs",
          "type": "checkbox",
          "default": false,
          "description": "Search all Langfuse configs (not just Chord)"
        }
      ]
    },
    {
      "id": "chord-uui-lookup",
      "name": "Chord UUI Lookup",
      "description": "Extract the pipe-delimited UUI (User-to-User Information) string from a Chord session. Accepts either a session ID (conv_*) or a search ID (UUID/trace ID). Returns parsed UUI segments, available variables, location GUID, DNIS, and call summary.",
      "command": "cd {{workDir}} && node \"C:/Users/mwoic/.claude/skills/chord-uui-lookup/lookup.js\" {{sessionId}}",
      "category": "agents",
      "inputs": [
        {
          "name": "sessionId",
          "label": "Session ID or Search ID",
          "type": "text",
          "required": true,
          "placeholder": "e.g. conv_8_+12132791621_1772224829364 or 12574d4f-5d82-483f-bd09-208fcb414ee0",
          "description": "Session ID (conv_X_+phone_timestamp) or search ID (UUID/trace ID) — auto-detected"
        }
      ]
    }
  ]
}
